[build]
  publish = "public"
  functions = "netlify/functions"

# Pretty API path: /api/sos  ->  the serverless function
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
